GMC
<% @cars.each do |car| %>
   
    <div>
    <%= link_to car.price, car_path(car.id), class: 'btn btn-primary' %>
    <%= link_to 'Edit', edit_car_path(car), class: 'btn btn-warning' %>
    <%= button_to 'Destroy', car_path(car.id), method: :delete %>

    </div>
  <% end %>
  <%= link_to 'Back to Home', root_path, class: 'btn btn-primary' %>
  <%= link_to 'create cars', new_car_path, class: 'btn btn-primary' %>
  